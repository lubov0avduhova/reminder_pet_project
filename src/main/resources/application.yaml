spring:
  application:
    name: reminder
  datasource:
    url: jdbc:postgresql://localhost:5432/reminder
    username: postgres
    password: 2212
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
